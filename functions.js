'use strict';

// 1. isHometown

function isHometown(town) {
    if (town === "San Francisco") { //just made an if statement here to compare the town to san fran
        return town === "San Francisco"; // i tried just returning True but it said True was undefined 
    }                                    // so i had to do that entire === statement
}

// 2. getFullName

function getFullName(first,last) {
    console.log(`${first} ${last}`) //sorry Hua Szu I had to sign off at 430PM today
}
   
// 3. calculateTotal

// Define your function here
